/**
 * RRROCA Smart Search — Fuse.js fuzzy search
 * Searches all site content client-side.
 * Search index is generated by Hugo at build time.
 */

(function() {
  'use strict';

  let searchIndex = null;
  let fuse = null;

  // Load Fuse.js from CDN
  function loadFuse() {
    return new Promise((resolve, reject) => {
      if (window.Fuse) { resolve(); return; }
      const script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/fuse.js@7.0.0/dist/fuse.min.js';
      script.onload = resolve;
      script.onerror = reject;
      document.head.appendChild(script);
    });
  }

  // Load search index (generated by Hugo)
  async function loadIndex() {
    if (searchIndex) return;
    try {
      const resp = await fetch('/index.json');
      searchIndex = await resp.json();
      await loadFuse();
      fuse = new Fuse(searchIndex, {
        keys: [
          { name: 'title', weight: 0.4 },
          { name: 'content', weight: 0.3 },
          { name: 'section', weight: 0.15 },
          { name: 'tags', weight: 0.15 }
        ],
        threshold: 0.3,
        includeScore: true,
        includeMatches: true,
        minMatchCharLength: 2,
        limit: 10
      });
    } catch (e) {
      console.warn('Search index not available:', e);
    }
  }

  function openSearch() {
    const overlay = document.getElementById('search-overlay');
    if (!overlay) return;
    overlay.classList.add('open');
    document.getElementById('search-input').focus();
    loadIndex();
    document.body.style.overflow = 'hidden';
  }

  function closeSearch() {
    const overlay = document.getElementById('search-overlay');
    if (!overlay) return;
    overlay.classList.remove('open');
    document.body.style.overflow = '';
  }

  window.openSearch = openSearch;
  window.closeSearch = closeSearch;

  // Search handler
  function handleSearch(e) {
    const query = e.target.value.trim();
    const results = document.getElementById('search-results');
    if (!results) return;

    if (!query || query.length < 2) {
      results.innerHTML = '<p class="search-hint">Start typing to search across all community content...</p>';
      return;
    }

    if (!fuse) {
      results.innerHTML = '<p class="search-hint">Loading search index...</p>';
      return;
    }

    const matches = fuse.search(query);
    if (matches.length === 0) {
      results.innerHTML = `<p class="search-hint">No results for "<strong>${query}</strong>". Try different keywords.</p>`;
      return;
    }

    results.innerHTML = matches.map(m => `
      <a href="${m.item.permalink}" class="search-result">
        <span class="search-result-section">${m.item.section || 'Page'}</span>
        <strong>${m.item.title}</strong>
        <p>${(m.item.content || '').substring(0, 150)}...</p>
      </a>
    `).join('');
  }

  // Bind events
  document.addEventListener('DOMContentLoaded', () => {
    const input = document.getElementById('search-input');
    if (input) {
      input.addEventListener('input', handleSearch);
    }
  });

  // Keyboard shortcuts
  document.addEventListener('keydown', (e) => {
    // Ctrl+K or Cmd+K to open search
    if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
      e.preventDefault();
      openSearch();
    }
    // Escape to close search
    if (e.key === 'Escape') {
      closeSearch();
    }
  });
})();
